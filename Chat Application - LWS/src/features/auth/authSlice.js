import { createSlice } from "@reduxjs/toolkit";


const initialState = {}


const authsSlice = createSlice({
    name: "authentication",
    initialState,
    reducers: {}
})


export default authsSlice.reducer

// export const { } = authsSlice.actions